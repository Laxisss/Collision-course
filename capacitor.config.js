// import { CapacitorConfig } from '@capacitor/cli';

const config = {
  appId: 'io.ionic.starter',
  appName: 'first_ionic_project',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
